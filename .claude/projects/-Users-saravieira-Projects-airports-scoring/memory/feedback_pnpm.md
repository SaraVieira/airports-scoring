---
name: use-pnpm
description: Always use pnpm instead of npm for package management
type: feedback
---

Use pnpm for all package installs and script runs, not npm.

**Why:** User preference — project uses pnpm.

**How to apply:** Use `pnpm install`, `pnpm add`, `pnpm dev` etc. instead of `npm install`, `npm run`, etc.
